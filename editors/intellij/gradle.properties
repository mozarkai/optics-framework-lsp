# Tracks pyproject.toml; release.yml asserts both match the tag.
pluginVersion=1.0.0

# The platform supplies kotlin-stdlib, so bundling a second copy would only risk a clash.
kotlin.stdlib.default.dependency=false
